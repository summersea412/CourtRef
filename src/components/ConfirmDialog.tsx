type ConfirmDialogProps = { onCancel: () => void; onConfirm: () => void; title?: string; message?: string; confirmLabel?: string; eyebrow?: string };
export function ConfirmDialog({onCancel,onConfirm,title='删除这个存档？',message='删除后，该存档及其中的数据将无法恢复。',confirmLabel='确认删除',eyebrow='DELETE ARCHIVE'}:ConfirmDialogProps){return <div className="dialog-backdrop" role="presentation" onClick={onCancel}><section className="dialog" role="alertdialog" aria-modal="true" aria-labelledby="confirm-title" onClick={e=>e.stopPropagation()}><p className="eyebrow">{eyebrow}</p><h2 id="confirm-title">{title}</h2><p>{message}</p><div className="dialog-actions"><button className="button secondary" onClick={onCancel}>取消</button><button className="button danger" onClick={onConfirm}>{confirmLabel}</button></div></section></div>}
